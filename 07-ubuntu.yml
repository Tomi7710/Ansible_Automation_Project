name: Install HTTP server
hosts: n2
become: yes

tasks:
  - name: Install Apache
    apt: 
      name: apache2
      state: present

  - name: start apache
    service: 
      name: apache2
      state: started

  - name: Enable apache
    service: 
      name: apache2 
      enabled: yes   
...